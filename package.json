{
  "name": "nest-angular-ionic-mono-repo-template-landscapes",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b",
    "build:watch": "tsc -b -w"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "apps/*",
    "common"
  ],
  "prisma": {
    "schema": "common/src/prisma/schema.prisma"
  },
  "devDependencies": {
    "prisma": "^6.12.0"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0"
  }
}